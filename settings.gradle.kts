pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "moe-kyaw-aung-portfolio"
include(":androidApp")
include(":backend")
include(":shared")
